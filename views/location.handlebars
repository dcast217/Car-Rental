<div class="container">
    <h3>{{locationData.name}}</h3>
    <small>{{locationData.address}}</small>

    <br /><br />
    <p>Don't see what you're looking for? We're always adding new inventory so check back regularly!</p>
    <br />

    <h4>Vehicles available at this location:</h4>
    <div class="row text-center d-flex align-items-stretch">
        {{> cars locationData.vehicles}}
    </div>
    
</div>